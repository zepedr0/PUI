import { WithbodyFilterPipe } from './withbody-filter.pipe';

describe('WithbodyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WithbodyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
